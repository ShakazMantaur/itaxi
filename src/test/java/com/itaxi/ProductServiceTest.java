package com.itaxi;

import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class ProductServiceTest {

    // iTaxi comment
    // actually there is no need to test this class as service just passes data through
    // and there's no actual business logic
    // testing only Java method calls or DTO mappings doesn't make sense at all...

}
